# CognoDB feature probe

Generated by `npm run probe`. Every Cypher construct this application depends on, verified against a live CognoDB `c0` instance rather than assumed from documentation.

`PASS` = parses, executes, and returns the expected result against a known fixture graph. `n/a` = confirmed unsupported, and the design accounts for it.

| Area | Construct | Result | Why this build cares |
|---|---|---|---|
| Connectivity | `connect` | PASS | Liveness. Also backs the /api/health endpoint. |
| Connectivity | `dbms.components()` | FAIL | Identifies the engine and version behind CognoDB. Informational. |
| Schema — constraints and indexes | `CONSTRAINT … REQUIRE` | PASS | Neo4j 5 constraint syntax. The seed script needs one of REQUIRE/ASSERT. |
| Schema — constraints and indexes | `CONSTRAINT … ASSERT` | FAIL | Legacy Neo4j 4 syntax. The fallback if REQUIRE is rejected. |
| Schema — constraints and indexes | `CREATE INDEX` | PASS | MERGE without a backing index does a full label scan. On 0.5 vCPU that is fatal. |
| Schema — constraints and indexes | `SHOW CONSTRAINTS` | PASS | Lets the seed script verify its own schema step instead of assuming it worked. |
| Write primitives — what the seed script needs | `UNWIND $rows batch` | PASS | The only sane way to bulk load. One write per row would take hours. |
| Write primitives — what the seed script needs | `SET n += $map` | PASS | Lets the loader push a whole property bag per row instead of naming each field. |
| Write primitives — what the seed script needs | `MERGE + ON CREATE / ON MATCH` | PASS | Get-or-create is what makes the seed script idempotent and re-runnable. |
| Write primitives — what the seed script needs | `multi-label CREATE` | PASS | Identifier nodes carry two labels, e.g. :Device:Identifier. Decision 4 of the model. |
| Write primitives — what the seed script needs | `SET n:Label (add label)` | PASS | Alternative to a per-kind loader pass when adding :Identifier after MERGE. |
| Write primitives — what the seed script needs | `CREATE with $relType` | PASS | If honoured, reads need no interpolated relationship types at all. |
| Write primitives — what the seed script needs | `MERGE with $relType (hazard)` | WRONG | Expect WRONG. MERGE matches an existing relationship of another type instead of creating this one. The seed script therefore uses literal types. |
| Traversal — the design-critical group | `var-length *1..3` | PASS | The multi-hop requirement in the brief. Everything on the Investigation Canvas needs it. |
| Traversal — the design-critical group | `var-length, multi-type` | PASS | A ring mixes device / address / bank / IP hops in ONE pattern. Without type disjunction the whole model falls apart. |
| Traversal — the design-critical group | `undirected var-length` | PASS | Account -> identifier <- Account is traversed against edge direction on the second leg. |
| Traversal — the design-critical group | `shortestPath()` | PASS | Q3, the headline "awkward for SQL" query. A fallback exists if this is missing. |
| Traversal — the design-critical group | `allShortestPaths()` | PASS | Nice-to-have for Path Finder when several equally short routes exist. |
| Traversal — the design-critical group | `closed walk (expect 0)` | PASS | Confirms simple-path semantics. Q4 cannot use this form and uses the decomposed one below. |
| Traversal — the design-critical group | `cycle, decomposed` | PASS | Q4, circular money movement. The "no clean SQL equivalent" claim rests on this. |
| Traversal — the design-critical group | `type disjunction as $param` | PASS | Lets LINK_TYPES travel as $linkTypes instead of being interpolated into read queries. |
| Traversal — the design-critical group | `depth as $param (expect fail)` | EXPECTED-FAIL | Expected to FAIL. Confirms why depth uses a frozen map of pre-built queries, not concatenation. |
| Functions and clauses the queries use | `list comprehension over path` | PASS | How Q3 turns a path into the evidence chain the analyst reads. |
| Functions and clauses the queries use | `label filter comprehension` | PASS | Recovers the specific identifier kind (:Device) from a node also labelled :Identifier. |
| Functions and clauses the queries use | `reduce() over a path` | PASS | Sums the money moved around a transfer cycle. |
| Functions and clauses the queries use | `all() predicate over rels` | PASS | "Every leg is small" — the filter that separates a fraud loop from ordinary payments. |
| Functions and clauses the queries use | `pattern predicate in WHERE` | PASS | Modern replacement for the deprecated exists(). Used to test "is this account flagged". |
| Functions and clauses the queries use | `COUNT {} subquery` | PASS | Degree filter. This is how we exclude super-nodes (a college IP with 40 accounts) from traversals. |
| Functions and clauses the queries use | `CALL {} subquery` | PASS | Would let ring scoring run in one round trip instead of several. |
| Functions and clauses the queries use | `EXISTS {} subquery` | FAIL | More expressive alternative to the pattern predicate above. |
| Functions and clauses the queries use | `head(collect()) ordering` | PASS | The shortestPath fallback. Keeps the shortest PATH, which min(length(p)) would throw away. |
| Functions and clauses the queries use | `collect(DISTINCT …)` | PASS | Rolls the shared identifier values for a pair into one row in Q1. |
| Functions and clauses the queries use | `startNode / endNode / type` | PASS | Turns a path into the edge list the force-graph component consumes. |
| Functions and clauses the queries use | `datetime() temporal` | PASS | Decides whether timestamps are stored as temporal types or as ISO strings + epoch millis. |
| Functions and clauses the queries use | `duration.between()` | PASS | The dormant bust-out ring is a timing signature: N accounts drawing max within 72 hours. |
| Extensions — expected absent, confirmed not assumed | `APOC` | FAIL | If present, apoc.path.expand would give configurable traversals. Not counted on. |
| Extensions — expected absent, confirmed not assumed | `GDS` | FAIL | If present, gds.wcc would replace the union-find in the app layer. Not counted on. |
| Extensions — expected absent, confirmed not assumed | `full-text index` | PASS | Would enable fuzzy address matching instead of rule-based normalisation. |
